Chorinitializer
Posee:
	- PID
	- ChordProtocol
	- PAR_PROT
Funciones:
	ChordInitializer (constructor) -> llama a GetPid de Peersim.Configuration para asignar un PID
	Initializer -> Inicializa un nodo n y lo "joinea" al overlay.o
					Obtiene un ChordProtocol con el Pid de ChordInitializer
	Join -> El objetivo es crear un chordprotocol(al parecer es como un nodo dechord)
			Crea un generador de números aleatorios
			Crea un Peersim.Node
			Le asigna un PID
			Busca un sucessor
			setea variables bases dl nodo
			Después hace un ciclo en el que agrega sucesores hasta que el primer elemento de sus sucesores sea igual al id de si mismo (osea que no hayan más sucesores) o que el primer sucessor de la lista comparado con nuestro ID sea mayor

		
ChordProtocol
Posee:
	Constante "transport"
	Parameters - p (PID Y TID)
	lookUpMessages - arreglo de enteros
	index - entero
	predecesor - Peersim.Node
	fingerTable - arreglo de Peersim.Node
	successorList - arreglo de Peersim.Node
	chordId - entero muy grande (debe ser el id del nodo dentro del anillo)
	m - entero tamaño  de fingerTable
	succLSize - tamaño de lista de sucesores - entero
	prefix - string
	next - entero
	currentnode - entero
	varSuccList - entero (??)
	stabilizations - entero (??)
	fails - entero (??)

Funciones:
	ChordProtocol (constructor) -> asigna un prefijo a prefix
									En lookupmessage crea un  arreglo de tamaño 1 y le asigna 0
									Asigna TID a p prefijo.transport
	processEvent -> Procesa evento LookUP y evento FinalMessage
	Clone -> Clona a la instancia de ChordProtocol
	getLookupMessage -> retorna lookUpMessage
	stabilize -> no lo entiendo
	updateSuccesorList -> Espero que no esté el error aquí
	updateSuccesor -> aqui tampoco
	notify ->  un nodo le avisa a otro que será su predecesor, en caso de que no sean los mismos nodos y el nodo al que se le avisa no tenga predecessor
	idInab(a,b,id) -> compara que dos id de 2 nodos sean diferentes a una tercera
	find_succesor(id) -> Espero que aqui no este el error
	closes_preceding_node -> Espero que aquí no este el error

CrateNw 
Posee:
	pid
	constant "idLength"
	constant "protocol"
	constant "succListSize"
	idLength - entero
	successorLsize - entero
	fingSize - entero
	verbose - para debuggin ? - boolean

Funciones:
	CreateNw (constructor) -> obtiene PID y otro 2 enteros desde Peersim.Configuratin
	Execute -> 

